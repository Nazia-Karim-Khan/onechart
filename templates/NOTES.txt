1. Get the application URL by running these commands:

2. To check the status of the deployment, run:
  kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "springboot-app-chart.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

3. To get the logs of the application, run:
  kubectl logs --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "springboot-app-chart.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

4. To delete the deployment, run:
  helm delete {{ .Release.Name }} --namespace {{ .Release.Namespace }}

5. List of resources deployed:
  echo "Deployments:"
  kubectl get deployments -n {{ .Release.Namespace }} -o custom-columns=NAME:.metadata.name

  echo "StatefulSets:"
  kubectl get statefulsets -n {{ .Release.Namespace }} -o custom-columns=NAME:.metadata.name

  echo "Services:"
  kubectl get services -n {{ .Release.Namespace }} -o custom-columns=NAME:.metadata.name

  echo "PersistentVolumeClaims:"
  kubectl get pvc -n {{ .Release.Namespace }} -o custom-columns=NAME:.metadata.name

  echo "ConfigMaps:"
  kubectl get configmaps -n {{ .Release.Namespace }} -o custom-columns=NAME:.metadata.name

  echo "Secrets:"
  kubectl get secrets -n {{ .Release.Namespace }} -o custom-columns=NAME:.metadata.name

{{ .Release.Name }} has been deployed to your Kubernetes cluster. Happy Kubernetting!